# 🔗 חיבור האפליקציה "ניהול קו ייצור" למערכת Priority ERP

> מסמך עבודה לקראת אפיון ופגישה עם איש היישום של פריוריטי.
> מבוסס על סקירת קוד האפליקציה בפועל (גרסה 4.3.0).

---

## 1. תקציר מנהלים

האפליקציה היא מערכת ניהול רצפת ייצור (MES קלה): מעקב פקודות עבודה (פק"ע), שלבי עבודה,
מספרים סידוריים, דיווחי שעות של עובדים, תקלות, חוסרים ותוכניות עבודה יומיות.

מטרת האינטגרציה:

| כיוון | מה עובר | למה |
|---|---|---|
| **פריוריטי ← אפליקציה** | פק"עות, מק"טים, פרויקטים, כמויות, תאריכי יעד, חוסרים | להפסיק הקלדה כפולה — הפק"ע נפתחת בפריוריטי ומופיעה אוטומטית ברצפת הייצור |
| **אפליקציה ← פריוריטי** | דיווחי ייצור (השלמת שלבים), שעות עבודה, תקלות/אי-התאמות, יחידות שנמסרו | שפריוריטי ישקף בזמן אמת מה קורה ברצפה — תמחיר, מלאי בתהליך (WIP), התקדמות |

**נקודת מוצא חשובה:** פריוריטי היא "מקור האמת" (Source of Truth) לפק"עות, מק"טים ופרויקטים.
האפליקציה היא מקור האמת לדיווחי הביצוע ברצפה.

---

## 2. מה האפליקציה כיום (ארכיטקטורה)

| רכיב | פירוט |
|---|---|
| **Frontend** | אפליקציית web (React PWA) בקובץ יחיד `index.html`, מתארחת ב-GitHub Pages, עברית RTL, עובדת גם מהנייד |
| **Backend** | Supabase — מסד נתונים PostgreSQL בענן + REST API + עדכונים בזמן אמת (Realtime WebSocket) |
| **ריבוי חברות** | כל רשומה משויכת ל-`company_id` (מולטי-טננט) |
| **ייבוא/ייצוא** | ייבוא וייצוא Excel מלא כבר קיים (פק"עות, סידוריים, תקלות, חוסרים) |
| **הרשאות** | היררכיה: מנהל מערכת ← מנהל מחלקה ← ראש צוות ← עובד (לפי תפקיד: מרכיב, מלחימה, חיווט, טכנאי, מבקר איכות) |

### טבלאות הנתונים באפליקציה (Supabase)

| טבלה | תוכן | שדות עיקריים |
|---|---|---|
| `orders` | פקודות עבודה (פק"ע) | `wo_number` (מספר פק"ע — מפתח עסקי), `pn` (מק"ט), `description`, `qty`, `unit_price`, `project_id`, `received_date`, `target_date`, `status` (פתוחה/חלקית/הושלמה/חסומה), `stage_ids` (שלבי הייצור), `serials` (רשימת מספרים סידוריים), `shortages` (חוסרים), `defects` (תקלות), `delivered_serials` (יחידות שנמסרו) |
| `projects` | פרויקטים | `id`, `name` |
| `stages` | שלבי עבודה / מרכזי עבודה | שם, סדר, תפקיד מבצע, זמן תקן (דקות ליחידה), תעריף שעתי, מחיר לשלב |
| `stage_blocks` | תבניות מסלול (רצף שלבים לשימוש חוזר) | שם + רשימת שלבים |
| `work_logs` | דיווחי עבודה בפועל | עובד, פק"ע, שלב, מספר סידורי, שעת התחלה/סיום, הפסקות, משך בדקות, הושלם |
| `custom_tasks` | משימות חופשיות (לא בפק"ע) | עובד, שם, מחיר, זמנים |
| `app_users` | עובדים ומשתמשים | שם, תפקיד, מחלקה, מנהל ישיר, תעריף שעתי, תעריף מכירה |
| `departments` | מחלקות | שם, מנהל |
| `daily_plans` | תוכניות עבודה יומיות | עובד + תאריך + פק"ע + שלב + כמות יעד |

---

## 3. מיפוי ישויות: אפליקציה ↔ פריוריטי

> שמות המסכים הטכניים בפריוריטי משתנים בין התקנות (בפרט אם יש התאמות אישיות) —
> העמודה "מסך בפריוריטי" היא ההנחה המקובלת, **ויש לאמת אותה מול איש הפריוריטי** (ראה שאלות בסעיף 7).

| ישות באפליקציה | ישות בפריוריטי | כיוון סנכרון | הערות |
|---|---|---|---|
| פק"ע (`orders.wo_number`) | פקודת עבודה — מודול ייצור/MRP | פריוריטי ← אפליקציה | מספר הפק"ע בפריוריטי = המפתח המקשר |
| מק"ט (`orders.pn`) | פריט — `PART` / `LOGPART` (שדה `PARTNAME`) | פריוריטי ← אפליקציה | קריאה בלבד |
| כמות + תאריך יעד | כמות לייצור + תאריך אספקה בפק"ע | פריוריטי ← אפליקציה | |
| פרויקט (`projects`) | פרויקט / הזמנת לקוח (`ORDERS`) | פריוריטי ← אפליקציה | לקבוע: פרויקט באפליקציה = פרויקט בפריוריטי או הזמנת לקוח |
| שלבי עבודה (`stages`) | פעולות במסלול הייצור (Routing) | פריוריטי ← אפליקציה (חד-פעמי/תקופתי) | אפשר גם לנהל ידנית באפליקציה בשלב ראשון |
| מספרים סידוריים (`serials`) | מספרים סידוריים (`SERNUMBERS`) | דו-כיווני (לקבוע מי יוצר) | היום האפליקציה מייצרת S/N אוטומטית לפי קידומת |
| דיווחי עבודה (`work_logs`) | דיווח ייצור / דיווח שעות בפק"ע | אפליקציה ← פריוריטי | הערך המרכזי של האינטגרציה בכיוון החוזר |
| תקלות (`defects`) | אי-התאמות / מודול איכות | אפליקציה ← פריוריטי | כולל חומרה, סוג, אחראי, תאריכי פתיחה/סגירה |
| חוסרים (`shortages`) | חוסרי מלאי / דרישות רכש / MRP | פריוריטי ← אפליקציה | היום מוקלד ידנית; פריוריטי יודעת לחשב חוסרים |
| יחידות שנמסרו (`delivered_serials`) | תעודת משלוח (`DOCUMENTS_D`) / סגירת פק"ע | אפליקציה ← פריוריטי (או קריאה בלבד) | לקבוע אם האפליקציה מדווחת מסירה או רק מציגה |
| עובדים (`app_users`) | עובדים בפריוריטי | פריוריטי ← אפליקציה (רשות) | נדרש רק אם רוצים לדווח שעות לפי מספר עובד בפריוריטי |
| סטטוס פק"ע | סטטוס פק"ע בפריוריטי | דו-כיווני | נדרש מיפוי סטטוסים (סעיף 6) |

---

## 4. ארכיטקטורת האינטגרציה המומלצת

**אסור** לקרוא לפריוריטי ישירות מהדפדפן (חשיפת סיסמאות, CORS). נדרשת שכבת ביניים בצד שרת.
מכיוון שהאפליקציה כבר יושבת על Supabase — הפתרון הטבעי והזול ביותר:

```
┌─────────────┐     REST/OData v4      ┌──────────────────┐    REST + Realtime    ┌─────────────┐
│  Priority    │ ◄════════════════════► │ Supabase          │ ◄═══════════════════► │ האפליקציה   │
│  ERP         │   (HTTPS + הרשאות)     │ Edge Functions    │                       │ (דפדפן/נייד) │
│              │                        │ + PostgreSQL      │                       │             │
└─────────────┘                        └──────────────────┘                       └─────────────┘
      ▲                                        │
      │  אופציונלי: Webhook מחוקק עסקי         │  סנכרון מתוזמן (cron) כל 5–15 דק'
      └────────────────────────────────────────┘  + טבלת sync_log למעקב ותקלות
```

### רכיבי הפתרון

1. **Supabase Edge Function — `priority-sync`**: פונקציית שרת שרצה כל 5–15 דקות (מתוזמן):
   - מושכת מפריוריטי פק"עות חדשות/מעודכנות (OData `$filter` לפי תאריך עדכון אחרון) וכותבת ל-`orders`.
   - דוחפת לפריוריטי דיווחי עבודה ותקלות חדשים שנוצרו באפליקציה מאז הסנכרון הקודם.
2. **טבלת `priority_sync_log`**: כל ריצה נרשמת (מתי, כמה רשומות, שגיאות) — חובה לתפעול ותמיכה.
3. **עמודות קישור**: הוספת `priority_id` / `synced_at` לטבלאות `orders`, `work_logs`, `defects` — כדי לדעת מה כבר סונכרן ולמנוע כפילויות.
4. **סודות (Credentials)**: פרטי ההתחברות לפריוריטי נשמרים אך ורק כ-Secrets של ה-Edge Function — לעולם לא בקוד הלקוח.
5. **אופציונלי — Push מפריוריטי**: חוקק עסקי (Business Rule) בפריוריטי ששולח Webhook בעת פתיחת/עדכון פק"ע → עדכון מיידי במקום המתנה לסנכרון הבא.

### ממשק ה-API של פריוריטי (למי שמיישם)

- פריוריטי חושפת **REST API בתקן OData v4**. כתובת טיפוסית:
  `https://<שרת>/odata/Priority/tabula.ini/<שם_חברה>/<ישות>`
- אימות: Basic Authentication (משתמש+סיסמה) או Personal Access Token — תלוי גרסה.
- דוגמת קריאה (שליפת פק"עות שעודכנו מאז תאריך):
  `GET .../<ישות_פקע>?$filter=<שדה_עדכון> gt 2026-08-01T00:00:00Z&$select=...`
- כתיבה: `POST` (יצירה) / `PATCH` (עדכון) עם גוף JSON.
- **נדרש רישיון API בתשלום מפריוריטי** + משתמש ייעודי עם הרשאות למסכים הרלוונטיים (סעיף 5).

### חלופת ביניים ללא API — "שלב 0" (זמין כבר היום)

באפליקציה קיים ייבוא Excel מלא. אפשר להתחיל מחר בבוקר בלי שום פיתוח:
מחוללים בפריוריטי דוח פק"עות ל-Excel (עמודות: מספר פק"ע, מק"ט, תיאור, כמות, פרויקט, תאריכים, סטטוס)
→ מייבאים לאפליקציה בלחיצה. זה לא זמן-אמת ולא דו-כיווני, אבל חוסך הקלדה ומאפשר לבדוק את
המיפוי לפני שמשלמים על רישיון API.

---

## 5. מה צריך לקבל מצד פריוריטי — צ'קליסט

| # | פריט | ממי | הערות |
|---|---|---|---|
| 1 | גרסת פריוריטי + סוג התקנה (Priority Cloud / שרת מקומי) | איש הפריוריטי | משפיע על אופן הגישה לרשת |
| 2 | **רישיון REST API (OData)** | נציג המכירות של פריוריטי | רישיון בתשלום — לוודא עלות מראש |
| 3 | משתמש API ייעודי + סיסמה/טוקן | מנהל המערכת של פריוריטי | לא משתמש אישי של עובד |
| 4 | הרשאות למשתמש ה-API | איש הפריוריטי | קריאה: פק"עות, פריטים, פרויקטים, הזמנות. כתיבה: דיווחי ייצור, אי-התאמות בלבד |
| 5 | כתובת ה-Endpoint המלאה + שם החברה (Company) ב-URL | איש הפריוריטי | כולל סביבת **טסט** נפרדת לבדיקות |
| 6 | שמות טכניים של המסכים/ישויות | איש הפריוריטי | פקודות עבודה, דיווח ייצור, מספרים סידוריים, אי-התאמות — כפי שהם בהתקנה הספציפית |
| 7 | רשימת שדות חובה + שדות מותאמים אישית (Custom) בכל מסך | איש הפריוריטי | כדי שכתיבה לא תיכשל על שדה חובה חסר |
| 8 | מיפוי סטטוסים של פק"ע | מוסכם במשותף | סעיף 6 |
| 9 | מגבלות קצב (Rate Limits) וחלונות תחזוקה | איש הפריוריטי | כדי לכוון את תדירות הסנכרון |
| 10 | דרישות רשת: Allowlist של IP? תעודות? VPN? | IT + איש הפריוריטי | רלוונטי בעיקר בהתקנה מקומית |

---

## 6. החלטות מיפוי שצריך לסגור (בפגישת האפיון)

1. **מפתח מקשר**: מספר הפק"ע בפריוריטי ייכנס ל-`orders.wo_number` (ייחודי לחברה). ✔ מומלץ
2. **מיפוי סטטוסים** — באפליקציה יש 4 סטטוסים:

   | אפליקציה | פריוריטי (לקבוע) |
   |---|---|
   | פתוחה (`open`) | פק"ע משוחררת/בתהליך |
   | חלקית (`partial`) | דווח חלקית |
   | הושלמה (`complete`) | הסתיימה / נסגרה |
   | חסומה (`blocked`) | מוקפאת / מעוכבת |

3. **מי יוצר מספרים סידוריים** — היום האפליקציה מייצרת אוטומטית (`מקט-0001`...). אם S/N מנוהל
   בפריוריטי — האפליקציה תקבל אותם משם במקום לייצר.
4. **פרויקט = מה?** — האם "פרויקט" באפליקציה ממופה לפרויקט בפריוריטי, להזמנת לקוח, או לשדה אחר.
5. **רמת דיווח השעות** — האם פריוריטי מקבלת דיווח מפורט (עובד+שלב+יחידה+דקות) או מצטבר
   (סה"כ לפעולה בפק"ע). לאפליקציה יש את הפירוט המלא — עדיף לדווח מפורט אם המסך תומך.
6. **תעריפים ותמחיר** — באפליקציה יש תעריף שעתי לעובד ולשלב. האם התמחיר נשאר בפריוריטי
   (מומלץ) והאפליקציה מדווחת רק זמנים וכמויות.
7. **מחיקות** — פק"ע שבוטלה בפריוריטי: מומלץ לסמן באפליקציה כחסומה/מבוטלת, לא למחוק (שמירת היסטוריית דיווחים).

---

## 7. שאלות שאיש הפריוריטי צפוי לשאול — ותשובות מוכנות

> זה הסעיף לקחת לפגישה. התשובות מבוססות על הקוד בפועל.

**ש: "מה המערכת שלכם ובאיזו טכנולוגיה היא בנויה?"**
ת: אפליקציית Web לניהול רצפת ייצור (React PWA), עם מסד נתונים PostgreSQL בענן (Supabase).
הקריאות לפריוריטי יבוצעו **מצד שרת בלבד** (Supabase Edge Functions) — לא מהדפדפן.
אנחנו עובדים ב-REST/JSON ותומכים ב-OData v4 ללא בעיה.

**ש: "אתם רוצים רק לקרוא נתונים או גם לכתוב לפריוריטי?"**
ת: בשלב ראשון קריאה בלבד (פק"עות, פריטים, פרויקטים). בשלב שני כתיבה מצומצמת ומוגדרת היטב:
דיווחי ייצור/שעות ואי-התאמות בלבד. אנחנו **לא** נוגעים בהנהלת חשבונות, מלאי כספי או מסמכים פיננסיים.

**ש: "אילו מסכים/ישויות אתם צריכים?"**
ת: קריאה — פקודות עבודה, פריטים (מק"טים), פרויקטים/הזמנות לקוח, מספרים סידוריים, חוסרים (אם זמין).
כתיבה — דיווח ייצור בפק"ע ואי-התאמות. הרשימה המלאה עם מיפוי שדות נמצאת בסעיף 3 של המסמך הזה.

**ש: "באיזו תדירות ואיזה נפח נתונים?"**
ת: נפח קטן. סנכרון מתוזמן כל 5–15 דקות, בכל ריצה עשרות רשומות לכל היותר (רק מה שהשתנה —
נשתמש ב-`$filter` על תאריך עדכון). בנוסף סנכרון מלא לילי. אין משיכות מסיביות בשעות העבודה.

**ש: "מאיפה תגיע התעבורה? צריך לפתוח חומת אש?"**
ת: הקריאות יגיעו מ-Supabase (ענן, HTTPS בלבד). אם נדרש Allowlist של כתובות IP קבועות —
נעמיד Gateway עם IP סטטי. אם הפריוריטי שלכם בענן פריוריטי — מספיק לנו כתובת ה-Endpoint וטוקן.

**ש: "יש לכם משתמש API? אתם יודעים שזה רישיון בתשלום?"**
ת: כן, אנחנו מבקשים מכם משתמש אפליקטיבי ייעודי (לא משתמש של עובד) עם הרשאות מינימליות
לפי הרשימה בסעיף 5. את הרישיון הלקוח ירכוש מפריוריטי — נשמח שתעבירו הצעת מחיר.

**ש: "יש סביבת טסט? איך תבדקו בלי לסכן את הייצור?"**
ת: נבקש מכם גישה לחברת/סביבת טסט בפריוריטי. כל הפיתוח והבדיקות יבוצעו מולה. המעבר לסביבת
הייצור רק אחרי אישור שלכם, ובשלב ראשון בקריאה בלבד (Read-Only) — כך שאין שום סיכון לנתונים.

**ש: "מה המפתח המקשר בין המערכות?"**
ת: מספר פקודת העבודה של פריוריטי — הוא נשמר אצלנו בשדה ייחודי (`wo_number`). פריטים לפי
מק"ט (`PARTNAME`), מספרים סידוריים לפי S/N. בנוסף נשמור אצלנו את המזהה הפנימי של פריוריטי לכל רשומה מסונכרנת.

**ש: "מה קורה אם דיווח נכשל? איך מוודאים שלא יהיו כפילויות?"**
ת: יש לנו טבלת לוג סנכרון; כל רשומה מסומנת אצלנו אחרי אישור מפריוריטי (`synced_at`), וכישלון
גורר ניסיון חוזר בריצה הבאה. הדיווחים נושאים מזהה ייחודי שלנו — כך שאפשר לזהות כפילות גם בצד פריוריטי.

**ש: "מי מנהל את עצי המוצר והמסלולים — אתם או פריוריטי?"**
ת: פריוריטי. האפליקציה רק מציגה את שלבי העבודה ומדווחת ביצוע מולם. אם המסלולים מנוהלים
בפריוריטי — נשמח למשוך אותם; אם לא, ננהל את רשימת השלבים אצלנו וזה לא מפריע לאינטגרציה.

**ש: "האם תצטרכו פיתוח מיוחד בצד פריוריטי?"**
ת: השאיפה — אפס פיתוח: עבודה מול המסכים הסטנדרטיים דרך ה-API. שתי בקשות אפשריות בלבד:
(1) חוקק עסקי ששולח Webhook בפתיחת/עדכון פק"ע (אופציונלי, לשיפור זמן התגובה);
(2) אם מסך הדיווח הסטנדרטי לא מכסה — מסך/ממשק ייעודי לקליטת דיווחים.

**ש: "איך מטופלים עדכונים שקורים בשני הצדדים במקביל (Conflict)?"**
ת: אין באמת קונפליקט כי הבעלויות מוגדרות: שדות הגדרת הפק"ע (כמות, תאריכים, מק"ט) — פריוריטי
תמיד גוברת. שדות ביצוע (דיווחים, תקלות) — נוצרים רק אצלנו וזורמים אליכם. אין שדה שנערך בשני הצדדים.

**ש: "מה עם אבטחת מידע?"**
ת: פרטי ההתחברות לפריוריטי נשמרים כ-Secrets בצד השרת בלבד ואינם מגיעים לדפדפן. כל התעבורה
HTTPS. משתמש ה-API מוגבל להרשאות המינימום. אפשר גם להגביל אותו לחברה אחת בפריוריטי.

**ש: "כמה עובדים/משתמשים ידווחו דרך האפליקציה?"**
ת: (להשלים לפי המפעל — לדוגמה: כ-X עובדי ייצור, Y מנהלים). חשוב: העובדים לא צריכים רישיון
פריוריטי — הם עובדים באפליקציה, ורק משתמש ה-API האחד מתקשר עם פריוריטי.

---

## 8. שאלות שכדאי **לשאול** את איש הפריוריטי

1. איזו גרסת פריוריטי מותקנת, והאם ממשק ה-REST (OData) פעיל? אם לא — מה נדרש להפעלתו ומה העלות?
2. מהם השמות הטכניים אצלכם של: מסך פקודות העבודה, מסך דיווחי הייצור, מסך המספרים הסידוריים ומסך אי-ההתאמות? האם הם מסכים סטנדרטיים או מותאמים?
3. אילו שדות חובה יש במסך דיווח הייצור? (כדי שכתיבה שלנו לא תידחה)
4. האם מנוהלים אצלכם מסלולי ייצור (פעולות) לכל מק"ט? האם מדווחים היום ברמת פעולה?
5. האם מנוהלים מספרים סידוריים בפריוריטי, ובאיזה שלב הם נוצרים?
6. האם קיימת סביבת טסט, ואיך מקבלים אליה גישה?
7. מהי מדיניות מגבלות הקצב (Rate Limit) על ה-API אצלכם?
8. האם ניתן להגדיר חוקק עסקי ששולח Webhook (קריאת HTTP) בעת עדכון פק"ע?
9. מי אצלכם מאשר הרשאות למשתמש ה-API ומה לוח הזמנים לקבלתן?
10. האם יש אינטגרציות API קיימות אחרות שרצות על אותה סביבה? (כדי לא להפריע זו לזו)

---

## 9. שלבי יישום מומלצים

| שלב | תוכן | תלות | הערכה |
|---|---|---|---|
| **0** | ייבוא Excel מפריוריטי (קיים היום באפליקציה) — התחלת עבודה ובדיקת מיפוי | דוח Excel מפריוריטי | מיידי |
| **1** | רישיון API + משתמש + גישה לסביבת טסט | פריוריטי | תלוי בספק |
| **2** | Edge Function לקריאה: משיכת פק"עות/פריטים/פרויקטים לטסט → ואז לייצור (Read-Only) | שלב 1 | ימים בודדים |
| **3** | הוספת עמודות `priority_id`/`synced_at` + טבלת לוג סנכרון + סנכרון מתוזמן | שלב 2 | ימים בודדים |
| **4** | כתיבה חזרה: דיווחי ייצור ואי-התאמות לפריוריטי (טסט → ייצור) | אימות מסך דיווח + שדות חובה | ~שבוע |
| **5** | אופציונלי: Webhook מפריוריטי לעדכון מיידי; משיכת חוסרים; דיווח מסירות | לפי צורך | לפי צורך |

---

## 10. סיכונים ודגשים

- **אין לחשוף את פרטי ה-API של פריוריטי בקוד הלקוח.** האפליקציה היום היא קובץ סטטי שכל הקוד שלו
  גלוי בדפדפן — לכן כל תקשורת עם פריוריטי חייבת לעבור דרך פונקציית שרת בלבד.
- **הקשחת Supabase מומלצת לפני חיבור לייצור**: מדיניות ה-RLS הנוכחית פתוחה (anon), ומפתח ה-anon
  מוטמע בקוד. לפני שנתוני ERP אמיתיים זורמים פנימה — כדאי לצמצם הרשאות.
- **ייחודיות מספרי פק"ע**: הסנכרון יבצע Upsert לפי `wo_number` — לוודא שאין כפילויות היסטוריות.
- **אל תסנכרנו הכול ביום אחד**: להתחיל בקריאה בלבד, לתת לזה לרוץ שבוע, ורק אז לפתוח כתיבה.
- **בעלות על נתונים**: כל שדה שייך לצד אחד בלבד (פריוריטי = הגדרה, אפליקציה = ביצוע). שמירה על
  הכלל הזה מונעת 90% מבעיות האינטגרציה.

---

*נוצר אוטומטית מסקירת קוד המערכת — גרסת אפליקציה 4.3.0, אוגוסט 2026.*

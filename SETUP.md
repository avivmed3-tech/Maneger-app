# 📋 הנחיות הגדרת Google Sheet — ניהול קו ייצור

## שלב 1: יצירת Google Sheet חדש
1. היכנס ל-https://sheets.google.com
2. לחץ על **"גיליון אלקטרוני ריק"**
3. שנה את השם ל: **"ניהול קו ייצור — Data"**

## שלב 2: פתיחת Apps Script
(הגיליונות `data` ו-`users` ייווצרו אוטומטית)
1. בתפריט העליון: **הרחבות** → **Apps Script**
2. מחק את כל הטקסט ב-`Code.gs`
3. הדבק את הקוד הבא:

```javascript
const SS = SpreadsheetApp.getActiveSpreadsheet();

function getOrCreateSheet(name) {
  let sheet = SS.getSheetByName(name);
  if (!sheet) {
    sheet = SS.insertSheet(name);
    sheet.getRange("A1").setValue(name + "_json");
  }
  return sheet;
}

function doGet(e) {
  const type = (e && e.parameter && e.parameter.type) || "";
  
  if (type === "users") {
    const sheet = getOrCreateSheet("users");
    if (sheet.getLastRow() < 2) return ContentService.createTextOutput("[]").setMimeType(ContentService.MimeType.JSON);
    const data = sheet.getRange("A2").getValue();
    return ContentService.createTextOutput(data || "[]").setMimeType(ContentService.MimeType.JSON);
  }
  
  const sheet = getOrCreateSheet("data");
  if (sheet.getLastRow() < 2) return ContentService.createTextOutput("{}").setMimeType(ContentService.MimeType.JSON);
  const data = sheet.getRange("A2").getValue();
  return ContentService.createTextOutput(data || "{}").setMimeType(ContentService.MimeType.JSON);
}

function doPost(e) {
  try {
    const body = JSON.parse(e.postData.contents);
    
    if (body.action === "saveUsers") {
      const sheet = getOrCreateSheet("users");
      sheet.getRange("A1").setValue("users_json");
      sheet.getRange("A2").setValue(JSON.stringify(body.users));
      return ContentService.createTextOutput("ok").setMimeType(ContentService.MimeType.TEXT);
    }
    
    if (body.action === "save") {
      const sheet = getOrCreateSheet("data");
      sheet.getRange("A1").setValue("data_json");
      sheet.getRange("A2").setValue(JSON.stringify(body.payload));
      return ContentService.createTextOutput("ok").setMimeType(ContentService.MimeType.TEXT);
    }
    
    return ContentService.createTextOutput("unknown action").setMimeType(ContentService.MimeType.TEXT);
  } catch(err) {
    return ContentService.createTextOutput("error: " + err.message).setMimeType(ContentService.MimeType.TEXT);
  }
}
```

## שלב 4: פריסה (Deploy)
1. לחץ על **"פריסה"** (Deploy) → **"פריסה חדשה"** (New deployment)
2. בסוג: בחר **"אפליקציית אינטרנט"** (Web app)
3. הגדרות:
   - **תיאור**: ניהול קו ייצור API
   - **הרצה כ**: **אני** (Me)
   - **גישה**: **כל אחד** (Anyone)
4. לחץ **"פריסה"** (Deploy)
5. אשר הרשאות אם מתבקש
6. **העתק את ה-URL** שמוצג — זה ייראה כמו:
   ```
   https://script.google.com/macros/s/AKfycbw.../exec
   ```

## שלב 5: עדכון הקוד
1. פתח את `index.html`
2. חפש את השורה (בתחילת הקובץ):
   ```javascript
   const GS_URL="";
   ```
3. הדבק את ה-URL שהעתקת:
   ```javascript
   const GS_URL="https://script.google.com/macros/s/AKfycbw.../exec";
   ```
4. שמור

## שלב 6: העלאה ל-GitHub Pages
1. צור repository חדש ב-GitHub (Public)
2. העלה את כל הקבצים מתיקיית `app` (index.html, sw.js, manifest.json, icon-192.png, icon-512.png)
3. Settings → Pages → Branch: **main** → Folder: **/ (root)** → Save
4. האפליקציה תהיה זמינה ב: `https://USERNAME.github.io/REPO-NAME`

## 🔑 פרטי כניסה (משתמשי דמו)

המערכת נטענת עם מבנה ארגוני מלא: **2 מחלקות**, בכל מחלקה **מנהל מחלקה**,
**2 ראשי צוות**, ולכל ראש צוות **3 עובדים** — בנוסף לעורך וצופה לבדיקת כל סוגי המשתמשים.

| משתמש | סיסמה | תפקיד | מחלקה / כפיפות |
|--------|--------|--------|----------------|
| admin  | admin123 | 🛡️ מנהל מערכת | — |
| avi    | 1234   | 💼 מנהל מחלקה | מחלקת הרכבה |
| yossi  | 1234   | ⭐ ראש צוות | מחלקת הרכבה ← אבי |
| dana   | 1234   | 🔩 מרכיב מכני | ← יוסי |
| moshe  | 1234   | 🔧 מלחימה | ← יוסי |
| ronit  | 1234   | 🔌 חיווט | ← יוסי |
| noa    | 1234   | ⭐ ראש צוות | מחלקת הרכבה ← אבי |
| itay   | 1234   | 👷 עובד | ← נועה |
| galit  | 1234   | 🔩 מרכיב מכני | ← נועה |
| omer   | 1234   | 🔧 מלחימה | ← נועה |
| tamar  | 1234   | 💼 מנהל מחלקה | מחלקת בדיקות |
| david  | 1234   | ⭐ ראש צוות | מחלקת בדיקות ← תמר |
| lior   | 1234   | 🛠️ טכנאי | ← דוד |
| shira  | 1234   | 🔍 מבקר איכות | ← דוד |
| roi    | 1234   | 👷 עובד | ← דוד |
| michal | 1234   | ⭐ ראש צוות | מחלקת בדיקות ← תמר |
| yoav   | 1234   | 🛠️ טכנאי | ← מיכל |
| hadas  | 1234   | 🔍 מבקר איכות | ← מיכל |
| uri    | 1234   | 🔌 חיווט | ← מיכל |
| anat   | 1234   | ✏️ עורך | מחלקת הרכבה |
| viewer | 1234   | 👁️ צופה | — |

> ⚠️ **חשוב:** שנה סיסמאות לאחר ההתחברות הראשונה!

## 🔧 פתרון בעיות סנכרון

### הסנכרון לא עובד?
1. **פתח Console בדפדפן** (F12 → Console) — חפש הודעות `[SYNC]`
2. **בדוק שה-URL נכון** — פתח את ה-URL בטאב חדש, אתה צריך לראות `{}` או את הנתונים
3. **פרוס מחדש** — Apps Script → Deploy → Manage deployments → עריכה → New version → Deploy
4. **⚠️ חשוב מאוד:** אחרי כל שינוי בקוד של Apps Script — חובה **פריסה חדשה** (New deployment) ולא רק שמירה!
5. **נקה localStorage** — בדפדפן הפלאפון: הגדרות אתר → מחק נתונים, ואז רענן

### בדיקה ידנית:
פתח בדפדפן:
```
YOUR_URL?t=123
```
אם מחזיר `{}` — הנתונים עוד לא נשמרו. תעשה פעולה כלשהי באפליקציה ותבדוק שוב.

---

## 🆕 עדכון סכימת Supabase (תעריפים, סיסמאות וצפי מכירות)

הגרסה החדשה משתמשת בעמודות נוספות. אם אתה מקים פרויקט Supabase חדש,
הרץ את ה-SQL הבא ב-SQL Editor (במסד הקיים העמודות כבר נוספו):

```sql
ALTER TABLE public.app_users ADD COLUMN IF NOT EXISTS sale_rate numeric DEFAULT 0;      -- תעריף מכירה (שווי מכירת עובד) לשעה
ALTER TABLE public.app_users ADD COLUMN IF NOT EXISTS password_plain text;              -- סיסמה לצפייה ע"י מנהל
ALTER TABLE public.stages    ADD COLUMN IF NOT EXISTS hourly_rate numeric DEFAULT 0;    -- תעריף שעתי קבוע למרכז עבודה (שלב)
```

> ⚠️ **אבטחה:** העמודה `password_plain` שומרת את סיסמת העובד כטקסט גלוי כדי לאפשר
> למנהל לצפות בה. סיסמאות שנוצרו לפני העדכון מאוחסנות בהצפנה (hash) בלבד ולא ניתן
> לשחזרן — יש לאפס אותן דרך עריכת המשתמש כדי שיופיעו בכפתור הצפייה.

---

## 🆕 עדכון סכימה — תפקיד לשלב, זמן עבודה ותוכניות עבודה יומיות

הגרסה החדשה מוסיפה: שיוך תפקיד מבצע + זמן עבודה (דקות ליחידה) לכל שלב,
וטבלת תוכניות עבודה יומיות (שיבוץ עובד לפק"ע + שלב ליום מסוים). הרץ ב-SQL Editor:

```sql
-- שלב: תפקיד מבצע (מרכיב/טכנאי/מבקר/חיווט...) וזמן עבודה משוער ליחידה (דקות)
ALTER TABLE public.stages ADD COLUMN IF NOT EXISTS role_id text;
ALTER TABLE public.stages ADD COLUMN IF NOT EXISTS minutes numeric DEFAULT 0;

-- תוכניות עבודה יומיות — מנהל משבץ לעובד פק"ע + שלב ליום נתון
CREATE TABLE IF NOT EXISTS public.daily_plans (
  id text PRIMARY KEY,
  user_id text NOT NULL,
  user_name text,
  plan_date date NOT NULL,
  order_id text,
  stage_id text,
  target_qty numeric DEFAULT 0,
  serial_ids jsonb DEFAULT '[]'::jsonb,
  note text,
  done boolean DEFAULT false,
  created_by text,
  created_by_name text,
  created_at timestamptz DEFAULT now(),
  updated_at timestamptz DEFAULT now(),
  company_id uuid DEFAULT '00000000-0000-0000-0000-000000000001'::uuid REFERENCES public.companies(id)
);
ALTER TABLE public.daily_plans ADD COLUMN IF NOT EXISTS serial_ids jsonb DEFAULT '[]'::jsonb;  -- מספרים סידוריים ספציפיים ששובצו (ריק = לפי כמות/הכל)
CREATE INDEX IF NOT EXISTS daily_plans_company_user_date_idx ON public.daily_plans (company_id, user_id, plan_date);
ALTER TABLE public.daily_plans ENABLE ROW LEVEL SECURITY;
DROP POLICY IF EXISTS anon_all_daily_plans ON public.daily_plans;
CREATE POLICY anon_all_daily_plans ON public.daily_plans FOR ALL USING (true) WITH CHECK (true);
ALTER PUBLICATION supabase_realtime ADD TABLE public.daily_plans;
```

> 💡 **זמן משוער לסיום פק"ע:** המערכת מחשבת צפי לפי סכום זמני השלבים × יחידות,
> חלקי "יום עבודה נטו" (ברירת מחדל 8 שע', ניתן לשינוי במסך הפק"ע). אם הפק"ע נבנתה
> מבלוק עם זמנים מוגדרים — הזמנים מהבלוק קודמים לזמן השלב הכללי.

---

## 🆕 עדכון סכימה — זמן תקן לכל מק"ט

הגרסה החדשה מפרידה בין שלושה זמנים שונים:

| מושג | פירוש | מקור |
|------|--------|------|
| **זמן תקן** | הזמן שהוגדר **מראש** לכל מק"ט, שלב אחר שלב | נקבע ידנית באפליקציה (לשונית ⏱️ זמן תקן ובפועל ← ״⚙️ הגדרת זמני תקן״) |
| **זמן בפועל** | הזמן שנמדד באמת מדיווחי העובדים (מה שנקרא בעבר "זמן תקן") | מחושב מרישומי העבודה שהסתיימו |
| **זמן מכירה** | הדקות שהוקצו לשלב במחירון / בבלוק | שדה `minutes` בשלב או בבלוק |

זמן התקן נשמר בטבלה חדשה, `pn_standards`.

> ✅ **הטבלה כבר נוצרה** בפרויקט `production-line-manager`
> (`lwiurzkiojctwzllpnmr`) — אין צורך להריץ כלום. ה-SQL כאן הוא לתיעוד
> ולהקמת פרויקט Supabase חדש.

```sql
-- זמן תקן לכל מק"ט — דקות לכל שלב (jsonb: {"s1":28,"s2":52,...})
CREATE TABLE IF NOT EXISTS public.pn_standards (
  id text PRIMARY KEY,
  pn text NOT NULL,
  stage_minutes jsonb DEFAULT '{}'::jsonb,
  total_min numeric DEFAULT 0,          -- זמן כולל ליחידה, לשימוש כשאין פירוט לפי שלב
  note text,
  updated_by text,
  created_at timestamptz DEFAULT now(),
  updated_at timestamptz DEFAULT now(),
  company_id uuid DEFAULT '00000000-0000-0000-0000-000000000001'::uuid REFERENCES public.companies(id)
);
-- אינדקס חיפוש בלבד, בכוונה לא ייחודי: הלקוח שומר את כל הרשומות בכל שמירה,
-- ושגיאת כפילות הייתה משתקת את הסנכרון. כפילויות מאוחדות בצד הלקוח.
CREATE INDEX IF NOT EXISTS pn_standards_company_pn_idx ON public.pn_standards (company_id, pn);
ALTER TABLE public.pn_standards ENABLE ROW LEVEL SECURITY;
DROP POLICY IF EXISTS anon_all_pn_standards ON public.pn_standards;
CREATE POLICY anon_all_pn_standards ON public.pn_standards FOR ALL USING (true) WITH CHECK (true);
ALTER PUBLICATION supabase_realtime ADD TABLE public.pn_standards;
```

> 💡 אם בכל זאת הטבלה חסרה (למשל בפרויקט חדש) האפליקציה לא נשברת: זמני התקן
> יישמרו בזיכרון המקומי (localStorage) של הדפדפן בלבד ולא יסונכרנו בין
> מכשירים, ואחרי יצירת הטבלה הם ייסנכרנו אוטומטית בשמירה הבאה.

**איפה זה מופיע:** לשונית ⏱️ *זמן תקן ובפועל* (גרפים, טבלאות לפי מק"ט/שלב/עובד
והמסך להגדרת התקן), מסך הפק"ע (זמן תקן ליחידה וצפי לפי תקן), דוחות הצוות
(פער מול תקן ועמידה בתקן), וייצוא ה-Excel (גיליונות *זמן תקן מול בפועל*
ו-*הגדרות זמן תקן*, ועמודות חדשות בגיליונות פקודות עבודה, לוג עבודה וסיכום).

**נתוני דמו:** כפתור טעינת נתוני הדמו יוצר זמן תקן לכל אחד מ-8 המק"טים
(מזהים שמתחילים ב-`demo_std_`), כך שאפשר לראות מיד את ההשוואה בין תקן, בפועל ומכירה.
שמונת התקנים האלה כבר נזרעו למסד הקיים, כך שהתצוגה עובדת גם בלי לטעון דמו מחדש.
מחיקת נתוני הדמו מוחקת גם אותם.

---

## 🆕 ייבוא דוח פק"ע מ-Priority

מסך **📂 ייבוא / ייצוא Excel** מזהה לבד שני פורמטים: את התבנית של האפליקציה,
ואת דוח פקודות העבודה שיוצא מ-Priority. הזיהוי נעשה לפי שורת הכותרות בלבד, כך
ששינוי שם הגיליון או עמודה נוספת בסוף לא שוברים אותו.

זהו מסלול הביניים עד לחיבור מלא ל-Priority דרך Engini. הייבוא הוא חד-כיווני —
שום דבר לא נכתב בחזרה ל-ERP.

### העמודות שהדוח חייב לכלול

| עמודה ב-Priority | לאן זה נכנס |
|---|---|
| `מספר פק"ע` | מספר הפק"ע (שדה חובה — שורה בלעדיו מדולגת) |
| `מק"ט` | P/N של הפק"ע, וגם המפתח של זמן התקן |
| `תיאור מוצר` | תיאור הפק"ע |
| `פרויקט` | פרויקט (0 = ללא פרויקט) |
| `סניף` | מספר הסניף — החטיבה העסקית של הפק"ע (0 = ללא סניף) |
| `כמות` | כמות יחידות |
| `מחיר אלת'א בש'ח` | מחיר ליחידה |
| `תאריך שחרור` | תאריך קבלה |
| `ת. סיום יצור` | יעד אספקה (0 = ללא יעד) |
| `שלב בפק"ע` | סדר הפעולה במסלול — קובע את סדר השלבים (גם האיות הישן `שלה בפק"ע` נקרא) |
| `פעולה` / `תאור פעולה` / `מרכז עבודה` | הפעולה שהופכת לשלב בפק"ע |
| `ז.תקן ליח' בדק' למקט` | זמן תקן בדקות ליחידה, לכל שלב של המק"ט |
| `סטטוס פק"ע` | סטטוס הפק"ע (ראה למטה) |

### גיליון ״סידוריים״

אם הקובץ כולל גיליון שני עם המספרים הסידוריים (`פק'ע` · `מק'ט` · `SN` ·
`כ. הפק'ע`), הסידוריים נכנסים לפק"ע שלהם כמו שהם. Priority מנפיק לפעמים את אותו
מספר סידורי בשתי פק"ע שונות, ולכן הבדיקה לכפילות היא בתוך הפק"ע בלבד.

פק"ע שאין לה שורות בגיליון נשארת בלי סידוריים — וזה בסדר: היא נכנסת ל**מעקב לפי
כמות**, וההתקדמות שבקובץ נרשמת לה כחתימה לפי כמות (ראו הפרק על גרסה 4.6). אפשר
לסמן ״השלם סידוריים לפק"ע שאינן בגיליון״ אם רוצים בכל זאת לנהל אותה יחידה-יחידה;
הסידוריים האלה **לא** מגיעים מ-Priority, ויוחלפו בייבוא הבא שכן יכיל אותן.

### שורה לכל פעולה — לא לכל פק"ע

ב-Priority כל שורה בדוח היא **פעולה אחת** בפק"ע, ולכן פק"ע עם מסלול בן 8 שלבים
מופיעה ב-8 שורות שחוזרות על אותם נתוני כותרת. הייבוא מקפל אותן בחזרה: קיבוץ לפי
מספר פק"ע, נתוני הכותרת מהקבוצה, וכל `תאור פעולה` שונה הופך לשלב של אותה פק"ע.

### המסלול שייך לפק"ע

לכל פק"ע יש **המסלול שלה** — רק השלבים שבאמת מופיעים עבורה בקובץ, בסדר של
`שלב בפק"ע` (10, 20, 30 …). יש פק"ע עם שלב אחד ויש עם עשרים, ושתי פק"ע יכולות
להריץ את אותן שתי פעולות בסדר הפוך — ושתיהן נכונות. הסדר הזה הוא סדר המערך
`stage_ids` של הפק"ע, ושום מסך באפליקציה לא ממיין אותו מחדש מול רשימת השלבים
הכללית.

רשימת השלבים ב-**🔧 שלבים** היא רק המאגר: כל פעולה מוכרת מופיעה בה פעם אחת, כי
לכל שלב צריך זהות קבועה כדי לדווח עליו עבודה, להגדיר לו זמן תקן ולתת לעובד
הרשאה אליו. פק"ע לעולם לא מקבלת את כל הרשימה — רק את השלבים שלה.

הסדר של הרשימה הכללית (שקובע דוחות ומסננים חוצי-פק"ע) נלמד מהמסלולים עצמם:
המערכת קוראת מכל פק"ע מי בא לפני מי, ומסדרת לפי מה שכל המסלולים מסכימים עליו,
עם `שלב בפק"ע` כשובר שוויון.

### הסטטוסים

35 הסטטוסים של Priority מתורגמים לארבעת הסטטוסים של האפליקציה:

- **פתוחה** — הודפסה · שוחררה · ממתין ליצור · קדם יצור · הועבר לייצור · מאושרת לקיוט
- **חלקית** — בתהליך ייצור · חיווט · בתהליך סגירה/בקורת/קיוט · סיום יצור · ביקורת
  ראשונ/חלקית/סופית · ממתין למבקר/לאריזה · אריזה · ביצור קבמ · ממתין א.FAI ועוד
- **הושלמה** — פקע COC · נסגרה
- **תקועה** — בעיה הנדסית · בעית ח"ג/כלי חסר · חוסרים ביצור · חוסר נגדי · הוקפאה ·
  הוחזר לתיקון · בטיפול לקוח

סטטוס שאינו ברשימה משויך לפי מילות המפתח שבו, ומדווח כהערה בתצוגה המקדימה.

### מה אפשר לבחור לפני האישור

- **דלג על פק"ע סגורות** (ברירת מחדל: פעיל) — לא מייבא פק"ע בסטטוס COC / נסגרה
- **צור שלבים חסרים** — פעולה שאין לה שלב מקביל נפתחת כשלב חדש בסוף רשימת השלבים
- **צור פרויקטים חסרים** — קוד הפרויקט מ-Priority נפתח כפרויקט
- **ייבא זמני תקן למק"טים** — לטבלת `pn_standards`, דקות ליחידה לכל שלב
- **השלם סידוריים לפק"ע שאינן בגיליון** (ברירת מחדל: כבוי) — עם תקרה לכל פק"ע,
  כדי לא לנפח את הנתונים. לא חובה: בלעדיו הפק"ע נכנסת למעקב לפי כמות

### שיוך פעולות לשלבים

בתצוגה המקדימה יש טבלת שיוך: לכל `תאור פעולה` אפשר לבחור שלב קיים, שלב חדש, או
לדלג. ברירת המחדל היא שלב קיים בעל אותו שם, ואם אין — שלב חדש. השיוך נשמר מקומית,
כך שהקובץ של החודש הבא נופל על אותו מסלול בלי לחזור על העבודה.

### החלף הכל מול הוסף לקיימים

**החלף הכל** מוחק את הפק"ע הקיימות — גם מ-Supabase, לא רק מהמסך — ומחליף אותן
בקובץ. **הוסף לקיימים** מוסיף אותן לצד הקיימות. השלבים, הפרויקטים וזמני התקן
תמיד נוספים ולא מוחקים דבר.

---

## 🆕 גרסה 4.5 — מה תוקן ומה נוסף

### 1. המערכת קראה רק 1,000 שורות מכל טבלה

PostgREST (ה-API של Supabase) מגביל תשובה אחת ל-**1,000 שורות** (`Settings → API →
Max rows`). הקוד ביקש עמודים של 5,000 שורות, קיבל 1,000, והסיק ש"זה הכל" — ולכן
עצר בשורה האלף של כל טבלה.

מה זה אומר בפועל על המסד הקיים:

| טבלה | במסד | מה שנטען לפני התיקון |
|---|---|---|
| `orders` | 2,787 | 1,000 |
| `work_logs` | 56,382 | 1,000 |
| `pn_standards` | 1,763 | 1,000 |

זו הסיבה ל"יש הרבה פחות פק"ע ממה שייבאתי" ולכך שדיווחי ההתקדמות שנקלטו בייבוא
כמעט לא נראו במסכים.

**התיקון:** הקריאה מתקדמת לפי מספר השורות שבאמת חזרו, ולא לפי הגודל שביקשה, כך
שהתקרה של השרת מתגלה במקום להיות מונחת. כל הטבלאות עוברות דרך אותה קריאה מדפדפת.
אפשר להעלות את `Max rows` בהגדרות Supabase כדי לצמצם את מספר הבקשות — הקוד יעבוד
נכון בכל ערך.

**סנכרון שוטף:** קריאה מלאה של 56,000 רישומי עבודה היא 57 בקשות, וזה בסדר פעם
אחת בטעינה אבל לא כל 15 שניות. לכן הרענון התקופתי קורא רק את מה שיכול היה
להשתנות — רישומים פתוחים, רישומים מהיומיים האחרונים, והרישומים שהיו פתוחים
בסריקה הקודמת — ומאחד אותם לתוך מה שכבר נטען. אחת ל-5 דקות מתבצעת קריאה מלאה
שמיישבת גם מחיקות שנעשו במכשיר אחר.

### 2. זליגת נתונים בין חברות

שלושה באגים נפרדים:

1. **הטעינה הראשונה רצה לפני שידעו לאיזו חברה המשתמש שייך.** `setCompanyId()`
   נקרא רק אחרי ש-`dbLoadAll()` הסתיים, ולכן כל רענון דף טען את הנתונים של חברת
   ברירת המחדל — לא משנה לאיזו חברה המשתמש התחבר. מזהה החברה נקרא עכשיו
   מה-session השמור כבר בטעינת הקוד, לפני הבקשה הראשונה.
2. **כל כתיבה נחתמה בחברת ברירת המחדל.** הממירים `dbTo*` לא החזירו את
   `company_id` לאובייקט של האפליקציה, ולכן `orderToDb` וחבריו נפלו תמיד על
   `DEFAULT_COMPANY_ID`. עכשיו כל שורה נושאת את החברה שלה, ושורה חדשה נחתמת
   בחברה שעובדים בה. בנוסף, השמירה מסננת ושולחת רק שורות של החברה הנוכחית.
3. **יומן הפעילות נקרא בלי סינון חברה** — כל חברה ראתה את הפעילות של כולן.
   נוסף סינון, וכל רשומת יומן חדשה נשמרת עם `company_id`.

> 📌 במסד הנוכחי לא נוצרו נתונים "מעורבבים" שצריך לנקות: לחברות הנוספות אין ולא
> היו שורות משלהן. מה שנראה כמו ערבוב היה באג 1 — הצגה של נתוני חברת ברירת המחדל
> בזמן שהייתם מחוברים לחברה אחרת.

### 3. פק"ע ב"הושלמה" בלי שום יחידה מדווחת

אלה שני נתונים שונים, ועד עכשיו המסך הראה רק את התוצאה של הסתירה ביניהם:

- **הסטטוס** (פתוחה / חלקית / הושלמה / תקועה) מגיע כמו שהוא מעמודת `סטטוס פק"ע`
  של Priority בייבוא. הוא לא מחושב מדיווחי עבודה.
- **ההתקדמות** (`0/0`, סרגל ההתקדמות, ספירת היחידות) נספרת אך ורק מדיווחי עבודה
  באפליקציה, לפי מספר סידורי ולפי שלב.

במסד הנוכחי **ל-2,302 מתוך 2,787 הפק"ע אין אף מספר סידורי** — כולל **כל 824
הפק"ע שסטטוסן "הושלמה"**. בלי סידוריים אין על מה לדווח, ולכן המונה של הפק"ע האלה
יישאר 0/0 בכל מצב, ו"חלקית" ו"בעבודה" נראות זהות ל"הושלמה". זה קרה כי קובץ
ה-Priority יובא בלי גיליון ״סידוריים״ ובלי לסמן ״השלם סידוריים לפק"ע שאינן
בגיליון״.

מה נוסף כדי שזה יהיה גלוי:

- תגית על כל פק"ע: **🚫 ללא יחידות לדיווח** · **⚠️ פער מול הסטטוס** · **▶️ דווחה
  עבודה** (כשה-ERP עוד אומר "פתוחה"), עם הסבר בריחוף.
- שני מסננים חדשים במסך הפק"ע לצד ארבעת הסטטוסים, ושורת הסבר שסופרת כמה פק"ע הן
  ללא סידוריים.
- סרגל שני, **דיווחי שלבים** — כמה שלבים בכל היחידות דווחו מתוך כמה שצריך. סרגל
  היחידות זז רק כשיחידה מסיימת את כל המסלול, ולכן פק"ע שבנויה למחצה נראתה בו 0.
- בכרטיס הפק"ע: כפתור **🔢 צור N סידוריים לפי הכמות** לפק"ע שאין לה סידוריים.
- בדאשבורד: הכרטיס נקרא עכשיו *פק"ע שטרם הסתיימו* ומציין כמה מהן בלי סידוריים.

### 4. אחזור לכל פעולה של מנהל

יומן הפעילות תיעד *שקרה משהו*; עכשיו נשמר גם **המצב שקדם לפעולה**, וזה מה שמאפשר
להחזיר אותה. כל רשומה מחזיקה, לכל אוסף שהיא נגעה בו, את השורות כפי שהיו ואת
המזהים שקיימים אחרי — אחזור כותב את הישנות בחזרה ומוחק את מה שהפעולה יצרה.

מה ניתן לאחזור: משתמשים (הוספה · עריכה · הפעלה/השבתה · **מחיקה**), מחלקות
(הוספה · עריכה · מחיקה · שינוי מנהל), פק"ע (יצירה · עריכה · מחיקה), שלבים
(הוספה · עריכה · מחיקה · שינוי סדר), בלוקים, פרויקטים, משימות, תוכניות עבודה,
רישומי עבודה, זמני תקן, וייבוא Excel/Priority שלם (כולל הפק"ע שהוחלפו והתקדמות
שנמחקה איתן).

**איפה:** 🛡️ ניהול מערכת → לשונית **↩️ יומן ואחזור**, וגם במסך 📋 היסטוריית
פעילות (מסנן "↩️ ניתן לאחזור"). הצילום נשמר מקומית בדפדפן שבו בוצעה הפעולה,
60 פעולות אחרונות; צילום גדול במיוחד (ייבוא של אלפי פק"ע) נשמר בזיכרון בלבד
ומסומן ״זמין עד לרענון הדף״.

**תוקן אגב כך:** מחיקת משתמש הסירה אותו מהרשימה המקומית בלבד ולא מ-`app_users`,
ולכן הוא חזר בטעינה הבאה. עכשיו השורה נמחקת בפועל, ואי אפשר למחוק את המשתמש
שאיתו מחוברים.

### 5. ניהול חברות ומחיקת חברה

🛡️ ניהול מערכת → לשונית **🏭 חברות**: רשימת החברות במערכת עם מספר הפק"ע,
הדיווחים, המשתמשים, השלבים והפרויקטים של כל אחת, ומי החברה שאליה אתם מחוברים.

מחיקת חברה מנקה את כל הטבלאות שלה לפי הסדר הזה, ורק אז את החברה עצמה (מפתחות
הזרים מצביעים *אל* `companies`, ולכן השורה לא יכולה ללכת ראשונה):

`work_logs → orders → custom_tasks → daily_plans → pn_standards → stage_blocks →
stages → projects → departments → audit_log → app_users → subscriptions →
companies`

המחיקה דורשת הקלדה של שם החברה, **אינה ניתנת לאחזור**, וחסומה לחברה שאליה אתם
מחוברים ולחברת ברירת המחדל.

---

## 🆕 גרסה 4.6 — מספר סניף (חטיבה עסקית) וחתימה לפי כמות

### עדכון סכימה — חובה להריץ בפרויקט Supabase חדש

> ✅ **כבר הורץ** בפרויקט `production-line-manager` (`lwiurzkiojctwzllpnmr`) —
> אין צורך לעשות דבר. ה-SQL כאן הוא לתיעוד ולהקמת פרויקט חדש.

```sql
-- מספר סניף (חטיבה עסקית) על כל פק"ע — מגיע מעמודת ״סניף״ בקובץ ה-Priority
ALTER TABLE public.orders ADD COLUMN IF NOT EXISTS branch text;
CREATE INDEX IF NOT EXISTS orders_company_branch_idx ON public.orders (company_id, branch);

-- מספרי הסניף המשויכים למחלקה — יכולים להיות יותר מאחד
ALTER TABLE public.departments ADD COLUMN IF NOT EXISTS branch_codes jsonb DEFAULT '[]'::jsonb;
```

> 💡 אם העמודות חסרות האפליקציה **לא נשברת**: השמירה מזהה עמודה שאינה קיימת
> (`PGRST204`), משמיטה אותה ושומרת את כל השאר. מספר הסניף יישאר בזיכרון המקומי
> של הדפדפן בלבד עד שה-SQL יורץ. בלי ההגנה הזו עמודה חסרה אחת הייתה מפילה כל
> שמירה של הטבלה — לא רק את השדה החדש.

### 1. מספר סניף = חטיבה עסקית

בקובץ ה-Priority יש עמודת **סניף** לצד כל מק"ט ופק"ע. המספר מייצג חטיבה עסקית:

| מספר | חטיבה |
|---|---|
| **51** | אזרחי |
| **52** | משולבות |
| **53** | תק"ש |
| **57** | מעבדות |

הרשימה אינה סגורה — מספר שאינו בה מוצג כ״סניף 61״, אפשר להקליד אותו ידנית בכל
מקום שבו בוחרים סניף, והוא נשמר ומסונן בדיוק כמו השאר.

**איפה זה מופיע:**

- **📋 פקודות עבודה** — מסנן **סניף (חטיבה)** בשורת המסננים, לצד הפרויקט
  והמיון. הרשימה מציגה כמה פק"ע יש בכל סניף, ויש גם ״ללא סניף״.
- **כרטיס הפק"ע ברשימה** — תג `🏢 51 · אזרחי` לצד המק"ט, כולל שם המחלקה בטולטיפ.
- **מסך הפק"ע** — התג מופיע בכותרת עם שמות המחלקות ששויכו למספר.
- **טופס פק"ע חדשה / עריכה** — שדה ״סניף (חטיבה עסקית)״ עם רשימת המספרים
  המוכרים ואפשרות להקליד מספר אחר.
- **ייבוא Priority** — תצוגה מקדימה של פילוח הקובץ לפי סניף לפני האישור.
- **ייצוא Excel** — עמודות ״סניף״, ״חטיבה״ ו״מחלקות הסניף״ בגיליון פקודות עבודה,
  פילוח לפי סניף בגיליון הסיכום, ועמודות הסניף בגיליון המחלקות.
- **תבנית הייבוא הריקה** — עמודת ״סניף״ בגיליון פקודות עבודה, כך שגם קובץ שממלאים
  ביד נושא את החטיבה.

> ℹ️ פק"ע שכבר קיימות במערכת מלפני העדכון נכנסו בלי מספר סניף (העמודה לא הייתה
> קיימת בזמן הייבוא). הן יקבלו אותו בייבוא ה-Priority הבא, או ידנית בעריכת הפק"ע.

### 2. מספרי סניף למחלקה

🛡️ ניהול מערכת → לשונית **🏢 מחלקות**: לכל מחלקה אפשר לשייך **מספר סניף אחד או
יותר** — לוחצים על המספרים ברשימה, או מקלידים מספר שאינו בה. השיוך מופיע על
כרטיס המחלקה, ומאותו רגע כל פק"ע של אותו סניף מציגה גם את שם המחלקה שלה.

השיוך נשמר בעמודה `departments.branch_codes`, ניתן לאחזור מיומן הפעולות ככל שינוי
אחר במחלקה, ומיוצא לגיליון ״מחלקות״ (מספרי סניף · חטיבות · מספר הפק"ע בסניפים).

### 3. חתימה לפי כמות — לפק"ע שאינן מנוהלות סיריאלית

עד עכשיו כל דיווח עבודה היה קשור ליחידה סיריאלית: ״יחידה #7 עברה הלחמה״. פק"ע
שאין לה מספרים סידוריים לא הייתה ניתנת לדיווח בכלל — כל המונים שלה נשארו 0 והיא
הופיעה כ״הושלמה · 0/0״ לנצח.

עכשיו יש דרך שנייה, לאותה שאלה רמה אחת למעלה: **כמה יחידות עברו את השלב**.

- פק"ע ללא סידוריים (ועם כמות) מקבלת בכרטיס שלה כרטיס **✍️ חתימה לפי כמות**:
  טבלה של שלבי המסלול עם ״נחתם״ · ״נותר״ · סרגל התקדמות, ולכל שלב שדה כמות
  וכפתור **חתום** (ברירת המחדל: כל מה שנותר).
- אפשר להזין גם **זמן בפועל בדקות** לאצווה (רשות). אם הוזן — הוא נכנס לחישובי
  זמן בפועל מול התקן; אם לא — החתימה נספרת כהתקדמות בלבד, בלי להמציא זמנים.
- כל חתימה נשמרת עם **מי חתם ומתי**, מוצגת בהיסטוריה של השלב (🕘) וניתנת לביטול
  אחת-אחת (🗑️ בטל).
- **מי חותם:** מנהל מערכת, עורך, מנהל מחלקה וראש צוות — על כל שלב; **עובד — על
  השלבים שבהרשאות שלו** (אותה הרשאת שלב שקובעת אילו יחידות מגיעות אליו בתור
  העבודה). שלב שאינו בהרשאותיו מוצג לו 🔒 ״לא בהרשאות שלך״ ואינו ניתן לחתימה.
- **העובד חותם מהמשתמש שלו**, כך שהדיווח נזקף אליו בדוחות ולא למנהל שחתם במקומו.
  עובד יכול לבטל חתימה של עצמו; מנהל יכול לבטל כל חתימה.
- החתימה מוגבלת לכמות שבפק"ע, כך ששלב לא יכול לעבור 100%.

**איפה העובד מדווח:** בשני מקומות, לפי מה שנוח לו —

1. **🔨 העבודה שלי** → כרטיס **״✍️ דיווח לפי כמות״**, שמרכז את כל הפק"ע ללא
   סידוריים שיש בהן שלב שלו שעדיין לא נחתם במלואו (עם מספר הדיווחים שנותרו לו
   בכל אחת). פק"ע כזו לא הופיעה שם קודם ולא יכלה להופיע — תור העבודה בנוי
   מיחידות, ולפק"ע בלי סידוריים אין יחידות.
2. **📋 פק"ע** → פתיחת הפק"ע → אותו כרטיס חתימה.

**איך זה נספר:** בדיוק כמו דיווח סיריאלי. ״יחידות שסיימו את כל המסלול״ הוא השלב
שנחתם הכי מעט (יחידה גמורה היא כזו שעברה את כל השלבים), ״דיווחי שלבים״ הוא סכום
החתימות מתוך כמות × שלבים. סרגלי ההתקדמות, הצפי לסיום והמסננים במסך הפק"ע קוראים
את זה כמו כל התקדמות אחרת.

**מבחינת הנתונים** חתימה כזו היא רישום עבודה רגיל בטבלת `work_logs` — בלי
`serial_id` ועם `bulk_count` = מספר היחידות. לא נוצרה טבלה חדשה, והחתימות
מסתנכרנות, מיוצאות ונשמרות ביומן כמו כל דיווח.

### 4. ההתקדמות מ-Priority נשמרת גם לפק"ע ללא סידוריים

בייבוא, פק"ע ללא סידוריים איבדה עד עכשיו את כל ההתקדמות שבקובץ (״כמות מדווחת״ /
״יתרה לבצוע״) — היא נכנסה כאילו לא נגעו בה. עכשיו ההתקדמות הזו נרשמת כחתימה לפי
כמות, כולל פק"ע שסטטוס ה-ERP שלהן ״הושלמה״ (שנחתמות במלואן). בקובץ האחרון זה
1,622 פק"ע שקיבלו את ההתקדמות האמיתית שלהן במקום 0%.

אותו דבר בתבנית הייבוא של האפליקציה: שורה בגיליון ״התקדמות״ של פק"ע ללא
סידוריים נרשמת כחתימה לפי כמות במקום להידחות.

### 5. הצ'יפ ״ללא יחידות לדיווח״ הפך ל״ללא סידוריים״

הצ'יפ במסך פקודות העבודה מסמן עכשיו פק"ע שאין לה סידוריים **ועדיין לא דווחה
עליה עבודה** — כלומר כזו שממתינה לחתימה הראשונה לפי כמות. ברגע שנחתמה עליה
כמות כלשהי היא מצטרפת לספירה הרגילה (פער מול הסטטוס / דווחה עבודה / תקין).
